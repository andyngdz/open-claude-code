import { clsx } from "clsx"

export const WORKBENCH_CARD_CLASS = clsx(
  "flex w-full flex-col gap-4",
  "rounded-lg border border-border p-6",
)

export const WORKBENCH_PANEL_CLASS = clsx(
  "mx-auto flex w-full max-w-7xl flex-col gap-6",
  "p-4 md:p-8",
)
